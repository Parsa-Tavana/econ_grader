{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment": "DEV-ONLY overrides, loaded only when ASPNETCORE_ENVIRONMENT=Development (dotnet run defaults to Development via launchSettings). Production never reads this file — that is why real-looking values are safe to keep here and NOT in appsettings.json. Prefer `dotnet user-secrets` for anything you actually care about.",
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=EconGrader;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;Encrypt=False"
  },
  "Jwt": {
    "SigningKey": "dev-only-signing-key-change-me-in-production-0123456789abcdef0123456789abcdef"
  }
}
